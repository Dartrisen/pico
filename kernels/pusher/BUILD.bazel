load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "pusher",
    hdrs = [
        "boris.hpp",
    ],
    deps = [
        "//data/field:field",
        "//data/particle:particle",
    ],
    copts = [
        "-O3",                           # Triggers vectorization pipelines
        "-fopenmp-simd",                 # Parses OpenMP SIMD directives without full OpenMP runtime cost
    ],
    visibility = ["//visibility:public"],
)