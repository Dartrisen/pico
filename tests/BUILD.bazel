load("@rules_cc//cc:defs.bzl", "cc_library", "cc_test")

package(default_visibility = ["//visibility:public"])

cc_test(
    name = "yee_maxwell_test",
    srcs = [
        "test_field.cpp"
    ],
    deps = [
        "//kernels/field:field",
        "@googletest//:gtest_main",
    ],
)

cc_test(
    name = "boris_test",
    srcs = [
        "test_boris.cpp"
    ],
    deps = [
        "//data/particle",
        "//data/field",
        "//kernels/pusher:pusher",
        "@googletest//:gtest_main",
    ],
)