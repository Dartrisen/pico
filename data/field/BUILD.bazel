load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "field",
    hdrs = glob(["include/*.hpp", "src/*.inl"]),
    deps = [
        "//data/grid:grid"
    ],
    includes = ["include"],
    include_prefix = "",
    copts = ["-O3", "-march=native"],
    visibility = ["//visibility:public"],
)
